Read [DESIGN.md](http://DESIGN.md) before implementing.

[DESIGN.md](http://DESIGN.md) is the source of truth. Never repeat or summarize it.

Implement one feature at a time.

Preserve the existing architecture (MVVM, SwiftData, async/await). Do not redesign unless explicitly requested.

Read only the minimum files required. Never scan the entire repository unless requested.

Modify only the requested files. Do not change unrelated code.

Extend existing code. Never regenerate or duplicate unchanged code.

Return only changed files and changed methods when possible. No explanations or summaries unless requested.

Update only affected sections of [DESIGN.md](http://DESIGN.md) when architecture changes. Otherwise leave it unchanged.

Reuse existing components before creating new ones.

Ask questions only if blocked; otherwise make reasonable assumptions.

Prefer minimal diffs over rewrites.

Minimize input and output tokens.